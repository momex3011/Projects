from pathlib import Path


def merge_video(frames_dir: Path, audio_path: Path | None, output_path: Path) -> Path:
    raise NotImplementedError("Connect ffmpeg stitching logic here after model inference.")
